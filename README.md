# StoryCanvas POC

Simple UI editor that uses your Storybook stories

Example usage:

```tsx
function App() {
  return (
    <StoryCanvas
      stories={{
        Header,
        Button,
      }}
    />
  )
}
```
